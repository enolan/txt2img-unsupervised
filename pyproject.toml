[project]
name = "txt2img-unsupervised"
version = "0.1.0"
description = ""
authors = [{ name = "Echo Nolan", email = "echo@echonolan.net" }]
license = { text = "BSD-3-Clause" }
readme = "README.md"
requires-python = ">=3.11.14,<3.12"
dependencies = [
    "CloseableQueue-py3>=0.9.2,<0.10",
    "dacite>=1.8.1,<2",
    "datasets>=3.0.1,<4",
    "diffrax>=0.7,<0.8",
    "einops>=0.7.0,<0.8",
    "flash-attention-jax",
    "flax>=0.12,<0.13",
    "imageio-ffmpeg>=0.4.8,<0.5",
    "infinidata",
    "internetarchive>=3.5.0,<4",
    "numpy>=2.0",
    "omegaconf>=2.3.0,<3",
    "optax",
    "orbax-checkpoint>=0.11,<0.12",
    "pillow>=9.5.0,<10",
    "pytest>=7.3.1,<8",
    "pytorch-lightning>=2.0.2,<3",
    "sortedcontainers>=2.4.0,<3",
    "torch",
    "transformers>=4.34.1,<5",
    "wandb>=0.17.9,<0.18",
    "warcat>=2.2.5,<3",
    "xdg-base-dirs>=6.0.1,<7",
    "rasterio>=1.4.3,<2",
    "seaborn>=0.13.2,<0.14",
]

[dependency-groups]
cuda = [
    "jax[cuda12]>=0.9.1,<0.10",
]
dev = [
    "flameprof>=0.4,<0.5",
    "hypothesis[numpy]>=6.92.2,<7",
    "ipython>=8.14.0,<9",
    "matplotlib>=3.7.1,<4",
    "memray>=1.11.0,<2",
    "mypy>=1.3.0,<2",
    "types-pillow>=9.5.0.4,<10",
    "types-tqdm>=4.65.0.1,<5",
    "pre-commit>=3.3.0,<4",
    "py-spy>=0.4.0,<0.5",
    "ruff>=0.15.8",
]

[tool.uv]
default-groups = ["dev", "cuda"]

[tool.uv.sources]
flash-attention-jax = { git = "https://github.com/enolan/flash-attention-jax.git", rev = "20621388795614de137c159a5c07abec5c475388" }
infinidata = { url = "https://github.com/enolan/infinidata/releases/download/v0.0.1-alpha2/infinidata-0.0.1a2-cp311-cp311-manylinux_2_34_x86_64.whl" }
torch = { url = "https://download.pytorch.org/whl/cpu/torch-2.0.1%2Bcpu-cp311-cp311-linux_x86_64.whl" }

[tool.ruff.lint]
extend-select = ["I", "B", "UP", "RUF"]
ignore = [
    "E731",
    "B007",  # unused-loop-control-variable — rename to _ is just style
    "B011",  # assert-false — we use assert False as unreachable markers
    "B023",  # function-uses-loop-variable — false positives with jax.vmap lambdas
    "B905",  # zip-without-explicit-strict — too noisy for JAX code
    "RUF001", "RUF002", "RUF003",  # ambiguous unicode — intentional math symbols
    "RUF005",  # collection-literal-concatenation — stylistic
    "RUF059",  # unused-unpacked-variable — rename to _ is just style
]

[tool.ruff.lint.isort]
# wandb creates a wandb/ directory that confuses ruff's third vs first party detection
known-third-party = ["wandb"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
